class InterestRateService {
    def double getInterestRate() {
        // This is when we should call out to a web service to retrieve today's interest rate

        // Instead of returning 0.0, we should return the current rate
        return 0.0
    }
}